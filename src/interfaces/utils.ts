export interface ProductInfo {
    name: string;
    quantity: number;
    totalValue: number;
}

export interface DateSales {
    _id: string;
    productsInfo: ProductInfo[];
}


export interface ProductInfo {
    name: string;
    quantity: number;
    totalValue: number;
}
